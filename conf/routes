# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~
GET         /users/:id      endpoint.AuthController.getUser(id: String)
POST       /signup          endpoint.AuthController.signup()
+nocsrf
PATCH       /users/:id      endpoint.AuthController.updateUser(id: String)
POST        /close          endpoint.AuthController.close()
